# CatMouse Game Project 협업 규칙

## 1. 코딩 전에 생각하기

**추측하지 않습니다. 혼란을 숨기지 않습니다. 선택지의 장단점을 드러냅니다.**

구현하기 전에 다음을 지킵니다.

- 전제와 가정을 명시적으로 밝힙니다. 확실하지 않다면 질문합니다.
- 해석이 여러 가지라면 가능한 해석들을 제시합니다. 아무 설명 없이 하나를 선택하지 않습니다.
- 더 단순한 접근법이 있다면 알립니다. 필요할 때는 사용자의 요청이나 현재 접근법에도 근거를 들어 이의를 제기합니다.
- 불명확한 부분이 있다면 작업을 멈춥니다. 무엇이 혼란스러운지 구체적으로 밝히고 질문합니다.

## 2. 단순함을 우선하기

**문제를 해결하는 최소한의 코드만 작성합니다. 추측에 기반한 코드는 작성하지 않습니다.**

- 요청받지 않은 기능을 추가하지 않습니다.
- 한 번만 사용하는 코드를 위해 추상화를 만들지 않습니다.
- 요청받지 않은 유연성이나 설정 가능성을 추가하지 않습니다.
- 발생할 수 없는 상황을 위한 오류 처리를 추가하지 않습니다.
- 200줄로 작성한 코드를 50줄로 해결할 수 있다면 다시 작성합니다.

항상 스스로 질문합니다. "숙련된 엔지니어가 이 구현을 과도하게 복잡하다고 판단할 것인가?" 그렇다면 단순화합니다.

## 3. 필요한 부분만 정확히 변경하기

**반드시 필요한 부분만 수정합니다. 자신이 만든 문제만 정리합니다.**

기존 코드를 수정할 때는 다음을 지킵니다.

- 인접한 코드, 주석, 서식을 임의로 개선하지 않습니다.
- 고장 나지 않은 부분을 리팩터링하지 않습니다.
- 다른 방식이 더 마음에 들더라도 기존 스타일을 따릅니다.
- 관련 없는 미사용 코드를 발견하면 사용자에게 알리되 삭제하지 않습니다.

자신의 변경으로 더 이상 사용되지 않는 코드가 생겼다면 다음을 지킵니다.

- 자신의 변경 때문에 미사용 상태가 된 import, 변수, 함수는 제거합니다.
- 사용자가 요청하지 않았다면 기존부터 존재하던 미사용 코드는 제거하지 않습니다.

판단 기준은 명확합니다. 변경된 모든 줄은 사용자의 요청과 직접 연결되어야 합니다.

## 4. 목표 중심으로 실행하기

**성공 기준을 정의하고, 검증될 때까지 반복합니다.**

작업 요청을 검증 가능한 목표로 바꿉니다.

- "검증 로직 추가" → "유효하지 않은 입력에 대한 테스트를 작성하고, 테스트가 통과하도록 구현"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 테스트가 통과하도록 수정"
- "X 리팩터링" → "리팩터링 전후에 테스트가 모두 통과하는지 확인"

여러 단계로 이루어진 작업은 다음 형식으로 짧은 계획을 먼저 제시합니다.

```text
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

강한 성공 기준은 작업자가 독립적으로 검증과 수정을 반복할 수 있게 합니다. "작동하게 만들기"처럼 약한 기준은 계속해서 추가 확인을 요구하게 됩니다.

## 기본 소통과 협업

- 모든 응답은 한국어 존댓말로 정중하고 실무적으로 작성합니다.
- 설명은 목록과 짧은 문단으로 명확하게 구성하며, 불필요한 감탄·과한 친근함·이모티콘을 사용하지 않습니다.
- 다인 협업 저장소이므로 현재 요청과 무관한 변경은 수정·되돌리기·스테이징하지 않습니다.
- 변경은 기능 또는 목적 단위로 좁게 유지하고, 코드·패키지·설정·문서 변경을 구분해 검토합니다.

## 저장소와 문서 구조

```text
docs/                   팀 공유 기획·기술·제출 문서
CatMouse Game Project/  Unity 프로젝트 루트
.codex/                 Codex 협업 설정과 프로젝트 스킬
```

- Unity는 `CatMouse Game Project/`를 프로젝트 루트로 열며, 셸에서는 공백이 포함된 경로를 항상 인용합니다.
- `docs/`에는 팀이 검토하고 커밋할 공통 문서만 둡니다. 개인 초안·메모·조사 원문·비공개 자료는 저장소 밖에서 관리합니다.

## Unity 작업 규칙

- 기본 탐색·수정 범위는 `Assets/`, `Packages/`, `ProjectSettings/`로 제한합니다.
- `Library/`, `Logs/`, `Temp/`, `Obj/`, `Build/`, `Builds/`, `UserSettings/`, `MemoryCaptures/`, `Recordings/`, `.vs/`, `.idea/`는 생성물 또는 개인 환경이므로 필요한 진단 외에는 읽거나 수정하거나 커밋하지 않습니다.
- Unity Editor가 열린 동안 프로젝트 루트와 `Assets/`, `Packages/`, `ProjectSettings/`를 이동하거나 이름을 바꾸지 않습니다.
- 에셋을 이동하거나 이름을 바꿀 때는 대응하는 `.meta` 파일을 함께 유지합니다.
- `Library/PackageCache`는 수정하지 않습니다. 패키지 변경 시 `Packages/manifest.json`과 `Packages/packages-lock.json`을 함께 검토합니다.
- `ProjectSettings/`의 자동 변경은 빌드·입력·렌더링 영향을 확인하고 의도한 파일만 반영합니다.
- Unity C#은 `docs/graphify-excluded/technical-foundation/code-conventions-v0.1.md`와 `docs/graphify-excluded/technical-foundation/mvp-architecture-v0.1.md`를 따르며, 플레이 검증은 실제 Unity 프로젝트에서 수행합니다.

## Git과 외부 자료

- `Assets/`, `Packages/`, `ProjectSettings/`와 관련 `.meta`는 포함하고, Unity 캐시·로그·빌드 산출물·개인 설정은 `.gitignore`로 제외합니다.
- 다른 사람의 미완료 변경이 있는 작업 트리에서는 전체 스테이징이나 일괄 포맷을 하지 않습니다.
- 파일 이동은 Unity 종료 후 수행하고 Git이 이동으로 인식하는지 확인합니다.
- 외부 에셋·오픈소스·AI 생성물의 출처와 라이선스는 `docs/`의 제출 문서에 기록합니다.
- 커밋 메시지, 커밋 분리, 사전 검증은 `docs/conventions/commit-convention.md`를 따릅니다.

## Graphify

- `graphify-out/`은 재생성 가능한 로컬 산출물이므로 Git에 포함하지 않습니다.
- `/graphify` 요청은 설치된 Graphify 스킬을 우선 따르고, 기존 `graph.json`이 있으면 코드베이스 질문에 `query`, `path`, `explain`을 먼저 사용합니다.
- 코드 또는 코드 경로 변경 후 `graphify update .`로 AST 그래프를 갱신합니다.
- 문서·이미지·PDF는 의미 추출이 필요할 때만 `/graphify --update`로 갱신합니다.
